# Nomad → Vault integration — generated by Ansible
# Loaded on every node alongside server.hcl / client.hcl

vault {
  enabled = true
  address = "http://127.0.0.1:8200"

  default_identity {
    aud  = ["vault.io"]
    env  = false
    file = true
    ttl  = "1h"
  }
}
