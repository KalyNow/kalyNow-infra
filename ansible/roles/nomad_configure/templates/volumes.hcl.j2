# Nomad host volumes — generated by Ansible
# Loaded on every node

client {
{% for vol in nomad_volumes %}
  host_volume "{{ vol.path | basename }}_data" {
    path      = "{{ vol.path }}"
    read_only = false
  }

{% endfor %}
}
